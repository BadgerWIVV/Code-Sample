SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;

USE WEA;
GO

SELECT
*
FROM
INFORMATION_SCHEMA.COLUMNS
WHERE
1 = 1

AND TABLE_NAME = 'MEDIMPACT_CLAIMS'
AND TABLE_SCHEMA = 'DN'
--AND COLUMN_NAME = ''
ORDER BY
 TABLE_NAME
,TABLE_SCHEMA
,COLUMN_NAME